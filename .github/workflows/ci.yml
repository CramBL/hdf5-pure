name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch: {}
  schedule:
    - cron: "0 3 1 * *"

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  test:
    name: test / ${{ matrix.os }} / ${{ matrix.config.name }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        config:
          - name: default
            flags: ""
          - name: full
            flags: --features "serde zfp fast-deflate provenance ndarray"
        # Subsets of `full`, which already runs on every platform.
        include:
          - os: ubuntu-latest
            config: {name: serde, flags: --features serde}
          - os: ubuntu-latest
            config: {name: zfp, flags: --features zfp}
          - os: ubuntu-latest
            config: {name: ndarray, flags: --features ndarray}

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install Rust
        run: rustup toolchain install

      # Writer for the `ci` group: the widest dependency set in this workflow.
      - name: Cache Cargo
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          shared-key: ci
          save-if: ${{ github.ref == 'refs/heads/main' && matrix.config.name == 'full' }}
          cache-on-failure: true

      - name: Install tools
        uses: taiki-e/install-action@920ab1831fbf4fb3ef75c8ead83556c918bb7290 # v2.79.8
        with:
          tool: cargo-nextest

      - name: Tests
        run: cargo nextest run --locked --features __hdf5-bundled ${{ matrix.config.flags }}

      # rustdoc compiles doctests, so nextest cannot run them.
      - name: Doctests
        run: cargo test --locked --doc --features __hdf5-bundled ${{ matrix.config.flags }}

  lint:
    name: lint / ubuntu-latest
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install Rust
        run: rustup toolchain install

      - name: Cache Cargo
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          shared-key: ci
          save-if: false

      - name: Install tools
        uses: taiki-e/install-action@920ab1831fbf4fb3ef75c8ead83556c918bb7290 # v2.79.8
        with:
          tool: just

      - name: Formatting
        run: just fmt-check

      - name: Clippy
        run: just clippy

      # A debug build hides two things: `debug_assert!` still type-checks its
      # argument in release (issue #288), and `--release`-only lints never fire.
      - name: Check (release)
        run: just check-release

      - name: Rustdoc
        run: just doc

      - name: Cargo.lock is current
        run: cargo update --workspace --locked

  examples:
    name: examples / ubuntu-latest
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install Rust
        run: rustup toolchain install

      - name: Cache Cargo
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          shared-key: ci
          save-if: false

      - name: Install tools
        uses: taiki-e/install-action@920ab1831fbf4fb3ef75c8ead83556c918bb7290 # v2.79.8
        with:
          tool: just

      # Each example ends in assertions, so running them tests the public API.
      - name: Run every example
        run: just examples

  ci:
    name: ci
    runs-on: ubuntu-latest
    needs: [test, lint, examples]
    permissions:
      contents: none
    if: always()
    steps:
      - name: Fail if any dependency failed
        if: contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') || contains(needs.*.result, 'skipped')
        run: exit 1
